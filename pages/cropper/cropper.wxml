<image-cropper
	id="image-cropper" 
	limit_move="{{true}}" 
	disable_rotate="{{true}}" 
	width="{{width}}" height="{{height}}" 
	imgSrc="{{src}}" 
	bindload="cropperload" 
	bindimageload="loadimage" 
	bindtapcut="tapCutPre"
	bindtapcuticon="tapIcon"
	bindtapgrayicon="tapIcon">
</image-cropper>